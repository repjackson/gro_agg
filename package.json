{
	"dependencies": {
		"@babel/runtime": "^7.10.4",
		"request": "^2.88.2",
		"request-promise": "^4.2.6"
	},
	"scripts": {
		"start": "MONGO_URL=mongodb+srv://daoadmin:daoadminpassword@cluster1-etul3.mongodb.net/daodb  meteor --settings settings.json --port $IP:$PORT",
		"local": "meteor --settings settings.json"
	}
}
