template(name='user_edit_membership')
    .ui.padded.stackable.grid 
        .row 
            .sixteen.wide.column
                .ui.header
                    i.id.badge.outline.icon
                    | dao membership
        .row 
            .sixteen.wide.column
                .ui.green.button.pay_membership
                    |pay $10 for membership
                each transactions
                    .ui.segment
                        a.ui.header(href="/transaction/#{_id}/view") #{when}
                        |#{amount} amount
                        if charge 
                            .ui.header charge
                        |#{charge.result.id}
                        .ui.header $#{charge.result.amount}
                        if is_admin
                            +remove_button
                            +print_this
                // +user_edit_finance
                +single_user_edit key='referrer' label='referred by' direct=true
                if is_admin
                    .ui.header
                        i.shield.icon
                        |admin
                    +array_edit key='levels' label='levels' direct=true
                    +array_edit key='roles' label='roles' direct=true
